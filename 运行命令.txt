本地化运行：docsify serve ./

git init // 初始化一个仓库
git add -A // 添加所有文件到暂存区，也就是交给由git管理着
git commit -m " second commit" // 提交到git仓库，-m后面是注释
git remote add origin https://docsify.github.com/zhanminjie/docsify.git
git push -u origin master // 推送到远程myblogs仓库